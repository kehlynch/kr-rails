<div class="container">
  <div class="row justify-content-center">
    <% rufer_options.each do |contract, label| %>
      <input hidden=true name="game[action]" value="pick_contract" />
      <input hidden=true id="<%= contract %>" name="game[pick_contract]" type="checkbox" value="<%= contract %>" />
      <%= contract_button(contract, label) %>
    <% end %>
  </div>
</div>
