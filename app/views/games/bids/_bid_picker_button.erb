<input
 hidden=true
 id="valid-<%= type %>-<%= slug %>"
 name="game[<%= type %>]"
 type="checkbox"
 value="<%= slug %>"
/>
<%=
  button_tag(
    name,
    alt: name,
    type: 'button',
    class: 'btn btn-primary',
    onclick: "submitBid('#{slug}', '#{type}')"
  )
%>
