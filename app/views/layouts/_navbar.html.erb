<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <%= render 'layouts/navbar/main_dropdown' %>
      <li class="nav-item <%= 'active' if current_page?(home_path) %>">
        <% if @player&.id %>
          <%= link_to @player.name, home_path, class: "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item <%= 'active' if current_page?(scores_path) %>">
        <% if @match.present? %>
          <%= link_to 'scores', scores_path, class: "nav-link" %>
        <% end %>
      </li>
      <li class="nav-item <%= 'active' if current_page?(play_path) %>">
        <% if @game.present? %>
          <%= link_to 'play', play_path, class: "nav-link" %>
        <% end %>
      </li>

    </ul>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item <%= 'active' if current_page?(play_path) %>">
        <% if @player %>
          <%= link_to 'log out', logout_path, method: :post, class: "nav-link" %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
